Lab 1. Introduction to Containers
Objectives
By the end of this lab, you will:

Understand the fundamental concepts of containerization.
Learn about container isolation and portability.
Gain hands-on experience running a simple container using Podman.
Prerequisites
A Linux-based system (Ubuntu 20.04+ or Fedora recommended).
Podman installed (see Setup section for installation instructions).
Basic familiarity with the command line.
Setup
1. Install Podman
Run the following commands based on your Linux distribution:

For Ubuntu/Debian:
sudo apt-get update
sudo apt-get install -y podman
For Fedora/RHEL/CentOS:
sudo dnf install -y podman
Verify Installation:
podman --version
Expected Output:
podman version 4.x.x (or similar)

Lab Tasks
Task 1: Understanding Container Basics
Subtasks:
What is a Container?

A container is a lightweight, standalone executable package that includes everything needed to run an application (code, runtime, libraries, and settings).
Unlike virtual machines, containers share the host OS kernel, making them faster and more efficient.
Key Characteristics of Containers

Isolation: Containers run in isolated user spaces, preventing interference with other containers or the host system.
Portability: Containers can run consistently across different environments (development, testing, production).
Task 2: Running a Simple Container with Podman
Subtask 1: Pull a Container Image
Podman uses images from container registries (e.g., Docker Hub). Let's pull the official hello-world image:

podman pull hello-world
Expected Output:

Trying to pull docker.io/library/hello-world:latest...
...
Status: Downloaded newer image for hello-world:latest
Subtask 2: Run the Container
Execute the container:

podman run hello-world
Expected Output:

Hello from Docker!  
This message shows that your installation appears to be working correctly.
...
Subtask 3: Verify Container Execution
List running containers:

podman ps -a
Expected Output:
A table showing the hello-world container with status "Exited".

Task 3: Exploring Container Isolation
Run an Interactive Container
Launch an Alpine Linux container with an interactive shell:

podman run -it alpine sh
Expected Output:
You will enter the container's shell (/ # prompt).

Verify Isolation
Inside the container, run:

cat /etc/os-release
Expected Output:
Shows Alpine Linux details (different from the host OS).

Exit the container:

exit
Troubleshooting Tips
Permission Errors:

Run Podman with sudo if needed (e.g., sudo podman run hello-world).
Configure rootless mode for better security:
podman system migrate
Network Issues:

Ensure your system has internet access to pull images.
Use podman --log-level=debug for verbose logs.
Conclusion
In this lab, you:

Learned the core concepts of containers, including isolation and portability.
Successfully ran a hello-world container using Podman.
Explored container isolation by interacting with an Alpine Linux container.
Containers provide a powerful way to package and deploy software consistently across environments.

Next Steps
Explore multi-container applications with Podman Compose.
Learn about container orchestration with Kubernetes or OpenShift.
Additional Resources
Podman Official Documentation
Red Hat OpenShift Container Development Guide

